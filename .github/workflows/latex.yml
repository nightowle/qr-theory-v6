name: Legacy LaTeX Build (disabled)

on:
  workflow_dispatch:

jobs: 
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use 'latex-ci.yml' and 'deploy-latex.yml'."